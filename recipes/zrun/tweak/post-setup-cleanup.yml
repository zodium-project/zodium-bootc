---
# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json

modules:

 - type: script
   snippets:
      - "dnf5 autoremove -y"
      - "dnf5 clean all"
      - "dnf5 clean packages"
      - "dnf upgrade --refresh --setopt=install_weak_deps=False -y"
      - "rm -rf /etc/yum.repos.d/vscode.repo"
      - "dnf -y copr disable matinlotfali/KDE-Rounded-Corners"
      - "dnf5 autoremove -y"
      - "dnf5 clean all"
      - "dnf5 clean packages"