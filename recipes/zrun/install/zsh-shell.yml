---
# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json

modules:

  - type: dnf
    install:
       install-weak-deps: false
       packages:
         - zsh
         - zsh-autosuggestions
         - zsh-syntax-highlighting
         - starship
         - zoxide

  - type: script
    scripts:
      - set-zsh-useradd-default.sh