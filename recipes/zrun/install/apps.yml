---
# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json

modules:
  - type: dnf
    repos:
      copr:
        - scottames/ghostty
        - sneexy/zen-browser
    install:
      install-weak-deps: false
      packages:
          - dolphin
          - dolphin-plugins
          - ark
          - spectacle
          - ghostty
          - zen-browser
          - code
          - kde-partitionmanager
          - kde-connect
          - plasma-systemsettings
          - plasma-systemmonitor
          - plasma-discover-kns
          - plasma-discover-notifier
          - plasma-discover-rpm-ostree
          - plasma-discover-flatpak
          - plasma-discover
          - kinfocenter
          - input-remapper

  - type: script
    snippets:
        - "rm -rf /etc/yum.repos.d/vscode.repo"
        - "dnf -y copr disable scottames/ghostty"
        - "dnf -y copr disable sneexy/zen-browser"
        - "rm -rf /etc/yum.repos.d/_copr:copr.fedorainfracloud.org:scottames:ghostty.repo"
        - "rm -rf /etc/yum.repos.d/_copr:copr.fedorainfracloud.org:sneexy:zen-browser.repo"