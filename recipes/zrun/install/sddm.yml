---
# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json

modules:

  - type: files
    files:
      - source: sddm-config
        destination: /

  - type: dnf
    install:
      install-weak-deps: false
      packages:
          - sddm
          - sddm-wayland-plasma
          - sddm-breeze
          - kde-settings-sddm