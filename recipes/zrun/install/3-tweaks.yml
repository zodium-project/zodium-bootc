---
# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json

modules:
 - type: files
   files:
     - source: system
       destination: /

 - type: script
   snippets:
      - "systemctl enable zodium-default-flatpak.service"